<!DOCTYPE html>
<html>
  <%- include('partials/head', { title: 'Verify Code' }) %>
  <body class="login-page">
    <div class="login-container">
      <h1>Verify Code</h1>
      <% if (result === 'valid') { %>
        <div class="verify-icon success">&#10004;</div>
        <p class="success">Code verified. Generated by <%= adminName %>.</p>
      <% } else if (result === 'invalid') { %>
        <div class="verify-icon error">&#10060;</div>
        <p class="error">Code is not valid.</p>
      <% } else if (result === 'rate') { %>
        <p class="error">Too many attempts. Please try again later.</p>
      <% } %>
      <form method="post" action="/verify">
        <label>Code:
          <input type="text" name="code" required maxlength="6">
        </label>
        <button type="submit">Verify</button>
      </form>
    </div>
  </body>
</html>

